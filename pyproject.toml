[project]
name = "sndz-play-mini"
version = "2.0.0"
description = "Minimal BPM-sorted local audio mini player for RUSH OPERATOR."
requires-python = ">=3.11"
dependencies = [
  "PySide6-Essentials>=6.7",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
