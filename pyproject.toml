[project]
name = "sono-play-mini"
version = "2.0.0"
description = "SONO PLAY MINI, a minimal BPM-sorted local audio mini player for RUSH OPERATOR."
requires-python = ">=3.11"
dependencies = [
  "PySide6-Essentials>=6.7",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
